spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Prod-ready-features
    username: root
    password: Harshu92.40%
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true

server:
  port: 9000

employeeService:
  base:
    BASE_URL: http://localhost:8080
